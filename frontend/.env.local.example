# Copy to .env.local:
#   cp .env.local.example .env.local

# Leave empty to use same-origin /api/* (recommended — no CORS issues)
NEXT_PUBLIC_API_URL=

# api-gateway must be running on this port
API_PROXY_URL=http://localhost:8080
