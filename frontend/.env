# Same-origin API calls via Next.js rewrite (avoids CORS) — requires api-gateway on :8080
NEXT_PUBLIC_API_URL=

# Next.js proxies /api/* → api-gateway during npm run dev
API_PROXY_URL=http://localhost:8080
