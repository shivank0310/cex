# Direct to api-gateway in dev (avoids Next.js proxy request pile-up)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Next.js proxies /api/* → api-gateway when NEXT_PUBLIC_API_URL is empty
API_PROXY_URL=http://localhost:8080